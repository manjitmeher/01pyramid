#include<stdio.h>
int main()
{
    int a,b,n;
    scanf("%d",&n);
    for(a=1; a<=n; a++)
    {
        for(b=1; b<=a; b++)
        {
            printf("01");
        }
        printf("\n");
    }
    return 0;
}